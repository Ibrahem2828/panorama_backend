# Staging deployment evidence

Status: **BLOCKED** on 2026-07-30.

No Coolify credentials, Staging URL, deployed commit SHA, immutable image digest, deployment timestamp, release-job output, TLS/WSS evidence, or managed PostgreSQL/Redis/local-media environment was provided to this workspace. The repository has a deployment runbook, but a runbook is not deployment evidence.

The first Staging record must include a redacted URL, commit SHA, image digest, image scan/SBOM/provenance artifact URLs, service states, release exit code, live/ready/startup responses, and resolution of any deployment failures.
